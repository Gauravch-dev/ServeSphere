<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.servesphere">
    <!-- 🌐 Core Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ServeSphere"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServeSphere">
        <activity
            android:name=".FindServiceActivity"
            android:exported="false" />
        <!-- 🚀 Launcher Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BookingAlertReceiver"
            android:exported="false" /> <!-- Registration -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- Main Dashboard -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Chatbot -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:label="ServeSphere Chatbot"
            android:theme="@style/Theme.ServeSphere" /> <!-- Booking Management -->
        <activity android:name=".AddBookingActivity" />
        <activity android:name=".ViewBookingsActivity" /> <!-- Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBm2Z6Ocjf0mw1lvnNVDRrh8DMzhtsrYqw" />
    </application>

</manifest>